

/**
 * A generic actor interface that ensures anything utilizing an actor must funtion properly
 * @author Peter Costantino
 *
 */
public interface Actor
{
	abstract public void act(); //interface for act method that has the animal perform an action
}
